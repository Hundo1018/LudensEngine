[project]
channels = ["conda-forge", "https://conda.modular.com/max"]
description = "Add a short description here"
name = "ludens-engine"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
max = ">=24.6.0,<25"
pygfx = ">=0.6.0,<0.7"

[pypi-dependencies]
glfw = ">=2.8.0, <3"
